<!DOCTYPE html>
<html>

<head>
    <title>Edit Note - LPU Notes</title>
    <link rel="icon" type="image/x-icon" href="/logo.png">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            --bg: #f4f6f9;
            --text: #1a1a1a;
            --text-muted: #666;
            --card-bg: #ffffff;
            --border: #eeeeee;
            --primary: #111111;
            --primary-invert: #ffffff;
            --input-bg: #ffffff;
        }

        body[data-theme='dark'] {
            --bg: #121212;
            --text: #f4f6f9;
            --text-muted: #a0a0a0;
            --card-bg: #1e1e1e;
            --border: #333333;
            --primary: #ffffff;
            --primary-invert: #111111;
            --input-bg: #2d2d2d;
        }

        body {
            font-family: 'Inter', Arial, sans-serif;
            background: var(--bg);
            color: var(--text);
            padding: 40px 20px;
            max-width: 600px;
            margin: 0 auto;
            transition: background 0.3s;
        }

        .header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 30px;
        }

        .logo {
            height: 50px;
        }

        h2 {
            margin: 0;
            font-size: 24px;
            font-weight: 800;
        }

        .edit-container {
            background: var(--card-bg);
            padding: 30px;
            border-radius: 20px;
            border: 1px solid var(--border);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
        }

        label {
            display: block;
            font-size: 13px;
            font-weight: 700;
            color: var(--text-muted);
            margin-bottom: 8px;
        }

        input,
        textarea {
            width: 100%;
            padding: 14px;
            margin-bottom: 20px;
            border: 1px solid var(--border);
            border-radius: 12px;
            font-size: 15px;
            background: var(--input-bg);
            color: var(--text);
            box-sizing: border-box;
            outline: none;
            font-family: inherit;
        }

        input:focus,
        textarea:focus {
            border-color: var(--primary);
        }

        button {
            width: 100%;
            padding: 14px;
            background: var(--primary);
            color: var(--primary-invert);
            border: none;
            cursor: pointer;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 700;
            transition: opacity 0.2s;
        }

        button:hover {
            opacity: 0.9;
        }

        .cancel-btn {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: var(--text-muted);
            text-decoration: none;
            font-size: 14px;
            font-weight: 600;
        }

        .current-image {
            margin-bottom: 20px;
            border-radius: 12px;
            overflow: hidden;
            border: 1px solid var(--border);
            max-width: 200px;
        }

        @media (max-width: 480px) {
            body {
                padding: 20px 15px;
            }

            .edit-container {
                padding: 20px;
            }
        }
    </style>
</head>

<body>
    <div class="header">
        <img src="/logo.png" alt="LPU Logo" class="logo">
        <h2>Edit Note</h2>
    </div>

    <div class="edit-container">
        <form action="/update-note/<%= note.id %>" method="POST" enctype="multipart/form-data">
            <label>Title</label>
            <input type="text" name="title" value="<%= note.title %>" required>

            <label>Content</label>
            <textarea name="content" rows="8" required><%= note.content %></textarea>

            <label>Change Attachment</label>
            <input type="file" name="media" accept="image/*" style="border: 1px dashed var(--border); padding: 10px;">

            <% if (note.image) { %>
                <label>Current Image</label>
                <div class="current-image">
                    <img src="<%= note.image %>" style="width: 100%; display: block;">
                </div>
                <% } %>

                    <button type="submit">Update Note</button>
        </form>
        <a href="/" class="cancel-btn">Cancel Changes</a>
    </div>

    <script>
        if (localStorage.getItem('theme') === 'dark') {
            document.body.setAttribute('data-theme', 'dark');
        }
    </script>
</body>

</html>