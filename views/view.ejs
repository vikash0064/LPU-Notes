<!DOCTYPE html>
<html>

<head>
    <title>
        <%= note.title %> - LPU Notes
    </title>
    <link rel="icon" type="image/x-icon" href="/logo.png">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            --bg: #f4f6f9;
            --text: #1a1a1a;
            --text-muted: #666;
            --card-bg: #ffffff;
            --border: #eeeeee;
            --primary: #111111;
            --primary-invert: #ffffff;
        }

        body[data-theme='dark'] {
            --bg: #121212;
            --text: #f4f6f9;
            --text-muted: #a0a0a0;
            --card-bg: #1e1e1e;
            --border: #333333;
            --primary: #ffffff;
            --primary-invert: #111111;
        }

        body {
            font-family: 'Inter', Arial, sans-serif;
            background: var(--bg);
            color: var(--text);
            padding: 40px 20px;
            max-width: 700px;
            margin: 0 auto;
            transition: background 0.3s;
        }

        .header {
            display: flex;
            align-items: center;
            gap: 15px;
            margin-bottom: 30px;
        }

        .logo {
            height: 50px;
        }

        .view-container {
            background: var(--card-bg);
            padding: 40px;
            border-radius: 24px;
            border: 1px solid var(--border);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
        }

        .note-title {
            font-size: 32px;
            font-weight: 800;
            margin-bottom: 20px;
            color: var(--primary);
            line-height: 1.2;
        }

        .note-content {
            font-size: 17px;
            line-height: 1.8;
            color: var(--text);
            white-space: pre-wrap;
            margin-bottom: 30px;
        }

        .note-media {
            width: 100%;
            border-radius: 16px;
            margin: 20px 0;
            border: 1px solid var(--border);
            display: block;
        }

        .meta {
            border-top: 1px solid var(--border);
            padding-top: 20px;
            margin-top: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .btn-back {
            display: inline-block;
            margin-top: 30px;
            color: var(--text-muted);
            text-decoration: none;
            font-weight: 600;
            font-size: 15px;
        }

        .btn-edit {
            background: var(--primary);
            color: var(--primary-invert);
            padding: 10px 20px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 700;
            font-size: 14px;
        }

        @media (max-width: 480px) {
            body {
                padding: 20px 15px;
            }

            .view-container {
                padding: 25px 20px;
            }

            .note-title {
                font-size: 26px;
            }

            .note-content {
                font-size: 16px;
            }
        }
    </style>
</head>

<body>
    <div class="header">
        <a href="/"><img src="/logo.png" alt="LPU Logo" class="logo"></a>
        <h2 style="margin: 0; font-size: 24px; font-weight: 800;">View Note</h2>
    </div>

    <div class="view-container">
        <h1 class="note-title">
            <%= note.title %>
        </h1>
        <div class="note-content">
            <%= note.content %>
        </div>

        <% if (note.image) { %>
            <img src="<%= note.image %>" alt="Note Attachment" class="note-media">
            <% } %>

                <div class="meta">
                    <span style="font-size: 12px; color: var(--text-muted);">Note ID: <%= note.id %></span>
                    <a href="/edit-note/<%= note.id %>" class="btn-edit">Edit Note</a>
                </div>
    </div>

    <a href="/" class="btn-back">← Back to Dashboard</a>

    <script>
        if (localStorage.getItem('theme') === 'dark') {
            document.body.setAttribute('data-theme', 'dark');
        }
    </script>
</body>

</html>