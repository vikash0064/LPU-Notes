<!DOCTYPE html>
<html>

<head>
    <title>View Note</title>
    <link rel="icon" type="image/x-icon" href="/logo.png">
    <style>
        body {
            font-family: Arial, sans-serif;
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background: #f4f6f9;
        }
        .note-container {
            border: 1px solid #ccc;
            padding: 20px;
            border-radius: 5px;
            background-color: #f9f9f9;
        }
        h2 {
            margin-top: 0;
        }
        .content {
            white-space: pre-wrap;
        }
        a {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            color: #000000;
        }
        a:hover {
            text-decoration: underline;
        }
        @media (max-width: 700px) {
            body {
                max-width: 98vw;
                margin: 0;
                padding: 10px 2vw;
            }
            .note-container {
                padding: 10px 2vw;
            }
            h2 {
                font-size: 20px !important;
            }
            img[alt="LPU Logo"] {
                height: 36px !important;
            }
        }
        @media (max-width: 400px) {
            body {
                padding: 2px 1vw;
            }
            .note-container {
                padding: 6px 1vw;
            }
            h2 {
                font-size: 16px !important;
            }
        }
    </style>
</head>

<body>

    <div style="display: flex; align-items: center; gap: 15px; margin-bottom: 20px;">
        <img src="/logo.png" alt="LPU Logo" style="height: 50px;">
        <h2 style="margin: 0;">View Note</h2>
    </div>
    <div class="note-container">
        <h2>
            <%= note.title %>
        </h2>
        <p class="content">
            <%= note.content %>
        </p>

        <% if (note.image) { %>
            <% if (note.image.match(/\.(jpg|jpeg|png)$/i)) { %>
                <img src="/uploads/<%= note.image %>" alt="Note Media" style="max-width:100%;margin-top:15px;" />
                <% } else { %>
                    <a href="/uploads/<%= note.image %>" target="_blank">Download attached file</a>
                    <% } %>
                        <% } %>


                            <hr>
                            <p><small>ID: <%= note.id %></small></p>
    </div>

    <a href="/">Back to Home</a>

</body>

</html>